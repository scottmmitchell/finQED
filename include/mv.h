mv_calculate_mean(e, w);
mv_calculate_variance(V, w);
mv_calculate_st_dev(V, w);
mv_calculate_portfolio_given_mean_no_short_sales(e, V, r);
mv_calculate_portfolio_given_mean_unconstrained(e, V, r);
mv_calculate_portfolio_given_mean_min_max_constraints(e, V, min_weights, max_weights,
r);
